// Örnek bir payment servis fonksiyonu
export const processPayment = (paymentDetails) => {
    // Ödeme işlemleri burada yapılır
    console.log('Processing payment with details:', paymentDetails);
};
